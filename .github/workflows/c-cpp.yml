name: C/C++ CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ik2machine
    steps:
    - uses: actions/checkout@v4
    - name: configure
      run: pwd
    - name: make
      run: df
    - name: dirs
      mkdir ik2_test
